# Copy to .env and update values
# For device testing: replace localhost with your LAN IP (ipconfig | findstr IPv4)
# For production: use the Railway API URL
EXPO_PUBLIC_API_URL=http://localhost:4000
